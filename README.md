# Alloy TAM Project – Alexandra Torres

A simple web app with a Svelte/Vite frontend and a Node/Express backend demonstrating form validation, API calls, and persona-based responses.

## ✨ Features
- Svelte/Vite frontend form with validation
- Express API with clear outcomes (Approved/Denied/Unknown)
- JSON-based responses mapped to on-page UI
- Clean error handling and CORS setup

## 🚀 Quick Start (Local)
**Prereqs:** Node.js 18+

```bash
# 1) Clone
git clone https://github.com/atorres01-netizen/alloy-tam-project-alextorres
cd alloy-tam-project-alextorres

# 2) Install dependencies
npm install

# 3) Run backend (in one terminal)
npm run server

# 4) Run frontend (in another terminal)
npm run dev
